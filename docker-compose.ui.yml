services:
  attack-flow-ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
      args:
        AFB_BASE_URL: ${AFB_BASE_URL:-/}
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000/api/v1}
    ports:
      - "${UI_BIND_PORT:-8080}:80"
    restart: unless-stopped
